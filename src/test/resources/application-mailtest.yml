my-spring:
  mail:
    host: 127.0.0.1
    port: 3025
    username: springboot
    password: duke
    properties:
      mail:
        smtp:
          auth: true
          connection-timeout: 5000
          timeout: 5000
          write-timeout: 5000
          starttls:
            enable: true
    retry:
      initial-interval-ms: 1000
      max-interval-ms: 10000
      multiplier: 2.0
      maxAttempts: 3
      sleep-time-ms: 2000